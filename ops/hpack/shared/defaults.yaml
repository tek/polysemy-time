<<: !include extensions.yaml

dependencies:
  - name: base
    version: '>= 4 && < 5'
    mixin:
      - hiding (Prelude)

ghc-options:
  - -flate-specialise
  - -fspecialise-aggressively
  - -Wall
  - -Wredundant-constraints
  - -Wunused-packages
